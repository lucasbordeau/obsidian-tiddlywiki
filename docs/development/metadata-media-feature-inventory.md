# Metadata and media transport inventory

Checked against the official documentation on 2026-09-09. Executable cases are in `src/tests/metadata-codecs.test.ts`, `metadata-roundtrip.test.ts`, and `metadata-media.test.ts`; input fixtures are under `src/tests/samples/metadata/`.

The tests exercise the portable container and metadata codecs. They compare original fields, strings, UTF-8 bytes and decoded base64 bytes after conversion. Media playback, image rendering and filesystem attachment writes belong to host integration checks. The wikitext and Markdown suites cover image/embed syntax separately.

| Documented feature                                          | Implemented behavior and executable coverage                                                                                                                                                                                                                     | Source                                                                                                                                   |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| JSON tiddler arrays with string-valued fields               | Validate the complete container; reject nonstring fields and duplicate/empty titles. Normalize an absent optional body to an empty string. Preserve arbitrary field names and values.                                                                            | [Tiddler files](https://tiddlywiki.com/static/TiddlerFiles.html)                                                                         |
| `.tid` header/body and inline `text:`                       | Read LF, CRLF and CR; preserve body whitespace; support Unicode and interior spaces in field names. Reject duplicate fields and ambiguous inline/body text. Report headers requiring JSON to preserve embedded line breaks, colons in names, or edge whitespace. | [Tiddler files](https://tiddlywiki.com/static/TiddlerFiles.html)                                                                         |
| Standard and arbitrary fields                               | Preserve dates, creator/modifier, ordered `list` values, caption, class, draft fields, ordering fields, plugin metadata, external resource fields, Unicode names and custom multiline values.                                                                    | [Tiddler fields](https://tiddlywiki.com/static/TiddlerFields.html)                                                                       |
| Created/modified timestamps                                 | Preserve existing compact UTC strings and absent dates. Convert explicit ISO instants and valid calendar dates deterministically; reject rollover during normalization. Original YAML spelling is retained by the preservation record.                           | [Date format](https://tiddlywiki.com/static/DateFormat.html)                                                                             |
| YAML properties and tags                                    | Parse mappings structurally; retain comments and raw YAML for unchanged round trips. Emit tag arrays, read legacy scalar tags, preserve Unicode/nested tag identities, map normalization collisions reversibly.                                                  | [Obsidian properties](https://obsidian.md/help/properties), [Obsidian tags](https://obsidian.md/help/tags)                               |
| Native Markdown tiddlers                                    | Route `text/x-markdown` and `text/markdown` bodies directly. Merge native YAML properties with tiddler metadata while retaining the original source and respecting edits.                                                                                        | [Official Markdown plugin](https://tiddlywiki.com/plugins/tiddlywiki/markdown/)                                                          |
| Plain text, HTML, CSS, JavaScript, JSON, dictionary and CSV | Preserve the declared MIME and exact text; emit a `preserved-content-type` diagnostic. Include literal syntax markers, Unicode, NULs and line endings in transport tests.                                                                                        | [Content types](https://tiddlywiki.com/static/ContentType.html)                                                                          |
| Compound and Classic wikitext; unknown types                | Preserve source with a diagnostic. Only default/current TW5 wikitext declarations enter the TW5 parser.                                                                                                                                                          | [Content types](https://tiddlywiki.com/static/ContentType.html)                                                                          |
| PNG, JPEG/JPG, GIF, WebP, AVIF, BMP; ICO                    | Preserve base64 and metadata through JSON, `.tid`, and metadata-aware round trips. Exercise every byte value, odd payload lengths and Unicode/spaced file identities; run the existing JPEG fixture through the complete route.                                  | [Obsidian file formats](https://obsidian.md/help/file-formats), [TW content types](https://tiddlywiki.com/static/ContentType.html)       |
| SVG                                                         | Preserve XML as UTF-8 source, including Unicode, entities and literal markup characters.                                                                                                                                                                         | [TW content types](https://tiddlywiki.com/static/ContentType.html)                                                                       |
| PDF documents                                               | Preserve encoded bytes, MIME, title and metadata, including metadata edits.                                                                                                                                                                                      | [Obsidian file formats](https://obsidian.md/help/file-formats)                                                                           |
| MP3, WAV, OGG, FLAC, M4A, 3GP and audio WebM                | Exercise encoded-byte transport per MIME declaration. Also compare the complete existing MP3 fixture through JSON and opaque body routing.                                                                                                                       | [Obsidian file formats](https://obsidian.md/help/file-formats)                                                                           |
| MP4, video WebM, OGV, MOV and MKV                           | Exercise encoded-byte transport per MIME declaration.                                                                                                                                                                                                            | [Obsidian file formats](https://obsidian.md/help/file-formats)                                                                           |
| External image/audio/HTML resource fields                   | Preserve absolute, relative and percent-encoded `_canonical_uri` values and empty bodies. The codecs perform no network fetch.                                                                                                                                   | [External images](https://tiddlywiki.com/static/ExternalImages.html), [Tiddler fields](https://tiddlywiki.com/static/TiddlerFields.html) |
| Arbitrary document/binary types                             | Include DOCX and `application/octet-stream` as opaque transport cases.                                                                                                                                                                                           | [TW content-type field](https://tiddlywiki.com/static/ContentType.html)                                                                  |

The attachment matrix records explicit MIME declarations for transport tests; it is not an extension-to-MIME detection implementation. Synthetic binary cases establish byte preservation independently of media decoders. The existing JPEG and MP3 fixtures add realistic payload sizes. Separate `.canvas` JSON and `.base` YAML examples also verify preservation of their document containers. These extensions are included in the [Obsidian file format inventory](https://obsidian.md/help/file-formats).

Preservation records have a schema version, source identity, original and emitted body snapshots, and metadata snapshots. Exact source restoration requires matching identity, content type and unchanged emitted body. Metadata edits are applied independently. An edit creates a fresh record after consuming the previous one, keeping repeated conversion size bounded. A colliding user property/field keeps its original value; the record uses a numbered namespace suffix.

The suite also covers malformed/cyclic YAML, field-name collisions, optional timestamps, invalid MIME shapes, unknown preservation versions, changed identities, changed MIME declarations, deletion/addition of properties and mixed nested metadata. Host-specific attachment import/export and runtime visual verification remain separate planned work.
